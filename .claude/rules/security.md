# Security Conventions

- **No secrets needed**: Browser-only game, no API keys
- **localStorage only**: Game saves to browser localStorage
- **No external requests**: All assets bundled locally
- **Input validation**: Sanitize any user inputs (studio name, etc.)
